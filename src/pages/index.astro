---
// SSG — fully prerendered
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"
    />
    <meta name="generator" content={Astro.generator} />
    <meta name="theme-color" content="#0a0a0a" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <title>Menú AR</title>
  </head>

  <body>
    <div class="home">
      <header class="home__header">
        <div class="home__icon" aria-hidden="true">🍔</div>
        <h1 class="home__title">Menú AR</h1>
        <p class="home__subtitle">Elige cómo quieres ver tu plato en 3D</p>
      </header>

      <nav class="home__nav" aria-label="Experiencias AR">
        <a href="/mindar" class="nav-card">
          <span class="nav-card__icon" aria-hidden="true">📷</span>
          <div class="nav-card__body">
            <span class="nav-card__title">MindAR</span>
            <span class="nav-card__desc">Apunta al menú con la cámara para ver el modelo en AR</span>
          </div>
          <span class="nav-card__arrow" aria-hidden="true">›</span>
        </a>

        <a href="/model-viewer" class="nav-card">
          <span class="nav-card__icon" aria-hidden="true">🔮</span>
          <div class="nav-card__body">
            <span class="nav-card__title">Model Viewer</span>
            <span class="nav-card__desc">Explora el modelo 3D interactivo sin necesitar marcador</span>
          </div>
          <span class="nav-card__arrow" aria-hidden="true">›</span>
        </a>

        <a href="/ar-quicklook" class="nav-card">
          <span class="nav-card__icon" aria-hidden="true">🍎</span>
          <div class="nav-card__body">
            <span class="nav-card__title">AR Quick Look</span>
            <span class="nav-card__desc">Coloca el plato en tu mesa con AR nativo de iPhone</span>
          </div>
          <span class="nav-card__arrow" aria-hidden="true">›</span>
        </a>
      </nav>
    </div>
  </body>
</html>

<style>
  :root {
    --color-bg: #0a0a0a;
    --color-surface: #1a1a1a;
    --color-surface-hover: #222;
    --color-border: rgba(255, 255, 255, 0.1);
    --color-text: #f5f5f5;
    --color-muted: rgba(255, 255, 255, 0.5);
    --color-accent: #ff6b35;
    --radius-card: 1.25rem;
    --shadow-card: 0 8px 32px rgba(0, 0, 0, 0.5);
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  html, body {
    width: 100%;
    min-height: 100%;
    background: var(--color-bg);
    color: var(--color-text);
    font-family: system-ui, -apple-system, sans-serif;
    -webkit-font-smoothing: antialiased;
  }

  .home {
    display: flex;
    flex-direction: column;
    align-items: center;
    min-height: 100dvh;
    padding: 2rem 1.5rem 3rem;
    gap: 2.5rem;
  }

  .home__header {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    text-align: center;
    padding-top: env(safe-area-inset-top, 1rem);
  }

  .home__icon { font-size: clamp(3rem, 10vw, 4.5rem); line-height: 1; }

  .home__title {
    font-size: clamp(1.75rem, 6vw, 2.5rem);
    font-weight: 800;
    letter-spacing: -0.03em;
  }

  .home__subtitle {
    font-size: clamp(0.875rem, 3vw, 1rem);
    color: var(--color-muted);
  }

  .home__nav {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    width: 100%;
    max-width: 28rem;
  }

  .nav-card {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1.25rem 1.25rem 1.25rem 1.25rem;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-card);
    box-shadow: var(--shadow-card);
    text-decoration: none;
    color: inherit;
    transition: background 0.2s ease, border-color 0.2s ease, transform 0.1s ease;
    -webkit-tap-highlight-color: transparent;
    touch-action: manipulation;
  }

  .nav-card:hover {
    background: var(--color-surface-hover);
    border-color: rgba(255, 255, 255, 0.2);
  }

  .nav-card:active { transform: scale(0.98); }

  .nav-card__icon {
    font-size: 2rem;
    line-height: 1;
    flex-shrink: 0;
  }

  .nav-card__body {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    flex: 1;
    min-width: 0;
  }

  .nav-card__title {
    font-size: 1rem;
    font-weight: 700;
    letter-spacing: -0.01em;
  }

  .nav-card__desc {
    font-size: 0.8125rem;
    color: var(--color-muted);
    line-height: 1.4;
  }

  .nav-card__arrow {
    font-size: 1.5rem;
    color: var(--color-accent);
    font-weight: 300;
    flex-shrink: 0;
    line-height: 1;
  }
</style>
